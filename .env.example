# 1. Copy this file and rename the copy to exactly  .env
# 2. Paste your real key after the = sign (no spaces, no quotes).
# 3. Get a key at https://console.anthropic.com  →  API Keys.
#
# Your .env file is private and is never uploaded (it's in .gitignore).
ANTHROPIC_API_KEY=sk-ant-paste-your-key-here
